import 'package:flutter/material.dart';

class MedicationsSubSection extends StatelessWidget {
  const MedicationsSubSection({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [Text("Medications Subsection")],
    );
  }
}
