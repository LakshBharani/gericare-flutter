import 'package:flutter/material.dart';

class DocumentsSubSection extends StatelessWidget {
  const DocumentsSubSection({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [Text("Documents Subsection")],
    );
  }
}
